# SwissAirDry Port-Konfiguration
# Diese Datei enthält alle Ports, die von SwissAirDry verwendet werden.
# Der Assistent kann diese Ports einfach anpassen, um Konflikte zu vermeiden.

# =============================================================================
# API-Server-Ports
# =============================================================================
# Hauptport für den API-Server (Standard: 5000)
# Wird für die direkte API-Kommunikation verwendet
API_PORT=5000

# Standard-HTTP-Port (80)
# Wird für unverschlüsselte HTTP-Anfragen verwendet
API_HTTP_PORT=80

# Standard-HTTPS-Port (443)
# Wird für verschlüsselte HTTPS-Anfragen verwendet
API_HTTPS_PORT=443

# =============================================================================
# MQTT-Server-Ports
# =============================================================================
# MQTT-Broker-Port (Standard: 1883)
# Wird für die Kommunikation mit den ESP32-Geräten verwendet
MQTT_PORT=1883

# MQTT WebSocket-Port (Standard: 8083)
# Wird für Web-Browser-basierte MQTT-Verbindungen verwendet
MQTT_WSS_PORT=8083

# =============================================================================
# ESP32-Firmware-Update-Port
# =============================================================================
# Port für den ESP32-OTA-Update-Server (Standard: 8070)
# Wird für Firmware-Updates der ESP32-Geräte verwendet
ESP32_UPDATE_PORT=8070

# =============================================================================
# Nextcloud-Integration-Ports
# =============================================================================
# Nextcloud HTTP-Port (Standard: 8080)
# Wird für die Kommunikation mit dem Nextcloud-Server verwendet
NEXTCLOUD_HTTP_PORT=8080

# Nextcloud HTTPS-Port (Standard: 8443)
# Wird für verschlüsselte Kommunikation mit dem Nextcloud-Server verwendet
NEXTCLOUD_HTTPS_PORT=8443